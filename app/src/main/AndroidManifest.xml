<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="au.edu.curtin.gridgame">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".View.NavigationActivity2"></activity>
        <activity android:name=".View.OpeningScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".View.MarketActivity" />
        <activity android:name=".View.WildernessActivity" />
        <activity
            android:name=".SmelloScopeActivity"
            android:label="@string/title_activity_smello_scope"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".SmellOScopeActivity" />
        <activity android:name=".View.SmellActivity" />
    </application>

</manifest>